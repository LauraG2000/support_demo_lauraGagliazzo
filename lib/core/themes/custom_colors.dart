import 'package:flutter/material.dart';

class CustomColors {
  // Primary
  static const Color primary = Color(0x005DA356); 
  static const Color secondary = Color(0xFFC8E6C9); 
  static const Color primaryContainer = Color(0xFFE8F5E9);

  // Background
  static const Color background = Color(0xf0f0f0f0);

  // Text
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Cards / Surfaces
  static const Color card = Colors.white;
  static const Color cardBorder = Color.fromARGB(255, 124, 124, 124);


  // Icons / Accent
  static const Color icon = Color(0xFF2E7D32);
}
